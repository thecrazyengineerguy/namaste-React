// //JSX is a html like syntax used by developers to code in react. It is not HTML in JS.
// //No we are going to create React.createElement using JSX syntax.
// //JSX(transpiled before it reaches the JS) - Parcel - Babel.

// //React element
// const Title = () => <h1 id="head">Namaste react in JSX </h1>;

// //React functional Component-Lets create this
// /** Anything JS can be written in between curly parenthesis in JSX like i did in line 15*/
// const HeadingComponent = () => (
//   <div id="container">
//     {<Title />}
//     <h1 id="heading">Namaste react Functional Component </h1>
//   </div>
// );

// const root = ReactDOM.createRoot(document.getElementById("root"));

// root.render(<HeadingComponent />);
